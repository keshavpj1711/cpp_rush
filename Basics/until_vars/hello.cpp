#include <iostream>  
// Includes the iostream library, which defines cout, cin, and others

using namespace std; 
// Makes the std namespace available, so you don't need to write std:: before cout, cin, etc.

int main()
{
  cout << "Hello World!!" << endl;
  return 0;
}